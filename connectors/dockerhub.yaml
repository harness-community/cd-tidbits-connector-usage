# Docker Hub connector — push the image this pipeline builds and
# generate an imagePullSecret for the Kubernetes deploy.
# Create the Text Secret first:
#   id: dockerhub-pat  value: a Docker Hub access token (not your password)
#
# Then: Project Settings → Connectors → Docker Registry.

connector:
  name: dockerhub-connector
  identifier: dockerhubconnector
  description: ""
  # REPLACE
  orgIdentifier: <YOUR_ORG_IDENTIFIER>
  # REPLACE
  projectIdentifier: <YOUR_PROJECT_IDENTIFIER>
  type: DockerRegistry
  spec:
    dockerRegistryUrl: https://index.docker.io/v2/
    providerType: DockerHub
    auth:
      type: UsernamePassword
      spec:
        # REPLACE: your Docker Hub username
        username: <YOUR_DOCKERHUB_USER>
        passwordRef: dockerhub-pat
    executeOnDelegate: false
    proxy: false
    ignoreTestConnection: false
